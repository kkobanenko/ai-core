# Characterization closeout instruction

Complete the existing compatibility-characterization work package only:

- create and populate the persistent `docs/agent-bridge/` coordination channel;
- verify and record non-main branch, historical PR #2, governance drift,
  review-issue, and branch-protection evidence;
- rerun the full test suite and both read-only guards at final content;
- update the technical report and handoff;
- commit and push only
  `test/ai-core-compatibility-characterization-20260909`.

Do not start a new work package, create or merge a PR, change `main`, release,
tag, deploy, modify consumers/platform-control, or implement runtime behavior.
