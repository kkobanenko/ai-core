commands:
  test: python -m pytest -q
  lint: null
  typecheck: null
  smoke: python -m pytest -q
  compose_validate: null
  install_dev: pip install -e ".[dev]"
notes:
  python_requirement: ">=3.10"
  workstation_hint: "Prefer uv python 3.12 or /usr/bin/python3.10; avoid anaconda python3.9 on PATH"
